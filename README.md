# RMOUG-cloud-run-demo-app

Hello world demoing a build pipeline ending with an automatically deployed cloud run instance

```
my laptop > github.com > cloud source repo > cloud build > container registry > cloud run
```
